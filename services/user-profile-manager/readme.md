# user-profile-manager

Сервис управления профилем пользователя

## Работа с эталонным контекстом

### Получение эталонного контекста

`GET /reference-context`

Алгоритм работы:

1. 🚧

### Установка эталонного контекста

`POST /reference-context`

Алгоритм работы:

1. 🚧

## Работа с поисковыми запросами

### Получить список настроенных поисковых запросов

`GET /search-query/list`

Алгоритм работы:

1. 🚧

### Добавление нового поискового запроса

| Входной параметр  | Источник      | Описание                |
|-------------------|---------------|-------------------------|
| 📌 `{entityUuid}` | path-параметр | UUID поискового запроса |

`POST /search-query/{entityUuid}`

Алгоритм работы:

1. 🚧

### Обновление поискового запроса

| Входной параметр  | Источник      | Описание                |
|-------------------|---------------|-------------------------|
| 📌 `{entityUuid}` | path-параметр | UUID поискового запроса |

`PATCH /search-query/{entityUuid}`

Алгоритм работы:

1. 🚧

### Удаление поискового запроса

| Входной параметр | Источник      | Описание                |
|-📌 -----------------|---------------|-------------------------|
| `{entityUuid}`   | path-параметр | UUID поискового запроса |

`DELETE /search-query/{entityUuid}`

Алгоритм работы:

1. 🚧

### Получение поискового запроса

| Входной параметр | Источник      | Описание                |
|-📌 -----------------|---------------|-------------------------|
| `{entityUuid}`   | path-параметр | UUID поискового запроса |

`GET /search-query/{entityUuid}`

Алгоритм работы:

1. 🚧

# Контроллер управления статусами вакансий

Работает с PostgresSQL:

- База данных `joposcragent`
- Схема `job_postings`
- Таблица `postings`

## Изменение статуса оценки вакансии

`POST /job-postings/{jobPostingUuid}/evaluation-status/{status}`

| Входной параметр      | Источник      | Описание                                 |
|-----------------------|---------------|------------------------------------------|
| 📌 `{jobPostingUuid}` | path-параметр | Внутренний UUID вакансии                 |
| 📌 `{status}`         | path-параметр | Новый статус оценки (`EvaluationStatus`) |

Алгоритм работы:

1. Проверяет наличие в таблице `postings` строки с `uuid` = `{jobPostingUuid}`
   1. Если строка не найдена, возвращает `HTTP 404`
2. Обновляет поля найденной строки:
   1. `evaluation_status` = `{status}`
   2. `updated_at` = `now()`
3. При успешной записи в БД возвращает `HTTP 200`
4. При возникновении любого не перехваченного исключения возвращает `HTTP 500` с текстом исключения в теле ответа

## Изменение статуса рассмотрения вакансии

`POST /job-postings/{jobPostingUuid}/response-status/{status}`

| Входной параметр      | Источник      | Описание                                     |
|-----------------------|---------------|----------------------------------------------|
| 📌 `{jobPostingUuid}` | path-параметр | Внутренний UUID вакансии                     |
| 📌 `{status}`         | path-параметр | Новый статус рассмотрения (`ResponseStatus`) |

Алгоритм работы:

1. Проверяет наличие в таблице `postings` строки с `uuid` = `{jobPostingUuid}`
   1. Если строка не найдена, возвращает `HTTP 404`
2. Обновляет поля найденной строки:
   1. `response_status` = `{status}`
   2. `updated_at` = `now()`
3. При успешной записи в БД возвращает `HTTP 200`
4. При возникновении любого не перехваченного исключения возвращает `HTTP 500` с текстом исключения в теле ответа
